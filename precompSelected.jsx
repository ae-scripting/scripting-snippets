//Small snippet to precomp all selected layers into different comps//CC-BY, Nik Ska, 2014var activeComp = app.project.activeItem;var moveAllAttributes = true; //change to false if neededif(activeComp && activeComp instanceof CompItem){	var sel = activeComp.selectedLayers;	if(sel){		app.beginUndoGroup("Batch Precomp");		for(var s = sel.length-1; s>=0; s--){			var p = activeComp.layers.precompose([sel[s].index], sel[s].name+"_precomp", moveAllAttributes);			p.parentFolder = activeComp.parentFolder;		}		app.endUndoGroup();	}}