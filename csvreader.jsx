//This script is an example of parsing csv file into AE compsfunction findCompByName(compName){    for(var i=1;i<=app.project.numItems;i++){        if(app.project.item(i).name==compName && app.project.item(i) instanceof CompItem) return app.project.item(i);    }    return null}var textFile = File.openDialog ("Выберите файл с данными (.csv)","*.csv");if (textFile != null) {    var textLines = new Array();    textFile.open("r", "TEXT"); }while (!textFile.eof){    textLines.push(textFile.readln()); }