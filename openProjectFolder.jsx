//Opens Project Folder//CC-BY-SA Nik Ska, 2014var ddOpenProjectFolder = function(){    if($.os.substr(0,7).toLowerCase() == "windows") var prefix = "explorer ";    else var prefix = "open ";    var prjPath = app.project.file;    if(prjPath){        var prjFldr = new Folder(prjPath.path);        prjFldr = prjFldr.fsName;        system.callSystem(prefix + "'" + prjFldr + "'");    }    else alert("You should first save the project");}ddOpenProjectFolder()